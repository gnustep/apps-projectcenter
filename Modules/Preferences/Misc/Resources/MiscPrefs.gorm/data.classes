{
    "## Comment" = "Do NOT change this file, Gorm maintains it";
    FirstResponder = {
	Actions = (
	    "setEditor:",
	    "setDebugger:",
	    "setFullPathInFilePanels:",
	    "setPromptWhenQuit:"
	);
	Super = NSObject;
    };
    PCMiscPrefs = {
	Actions = (
	    "setEditor:",
	    "setDebugger:",
	    "setFullPathInFilePanels:",
	    "setPromptWhenQuit:"
	);
	Outlets = (
	    debuggerButton,
	    debuggerField,
	    deleteCache,
	    editorButton,
	    editorField,
	    fullPathInFilePanels,
	    miscView,
	    promptWhenQuit
	);
	Super = NSObject;
    };
}